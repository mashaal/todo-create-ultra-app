fragment TodoFragment on Todo {
  _id
  label
  list {
    ...ListFragment
  }
  project {
    ...ProjectFragment
  }
  tags {
    data {
      ...TagFragment
    }
  }
  repeat {
    ...RepeatFragment
  }
  dueDate
  startDate
  endDate
}